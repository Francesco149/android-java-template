template for a sane android java setup that doesn't rely on any ide or
xml layouts.

connect your device via usb and simply run

```
./build.sh
```

which will compile, sign and deploy the app. the apk is in ```bin/```

